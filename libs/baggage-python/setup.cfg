[metadata]
name = tekton-dag-baggage
version = 1.0.0
description = Role-aware W3C baggage / x-dev-session middleware for Flask

[options]
packages = tekton_dag_baggage
python_requires = >=3.10
install_requires =
    flask>=3.0.0
    requests>=2.31.0

[options.extras_require]
test = pytest>=7.0
