{{- if .Values.operator.enabled }}
{{- $teams := .Files.Glob "raw/team-crs/*.yaml" }}
{{- if eq (len $teams) 0 }}
{{- fail "operator.enabled requires Team CRs: run helm/tekton-dag/package.sh before helm template/install (raw/team-crs is empty)" }}
{{- end }}
{{- range $path, $_ := $teams }}
---
{{ $.Files.Get $path }}
{{- end }}
{{- end }}
