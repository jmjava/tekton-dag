---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: stacks.tektondag.io
spec:
  group: tektondag.io
  names:
    kind: Stack
    listKind: StackList
    plural: stacks
    singular: stack
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.valid
      name: Valid
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Stack is the Schema for the stacks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StackSpec defines the desired state of Stack (mirrors stacks/schema.json).
            properties:
              apps:
                description: Apps is the application DAG.
                items:
                  description: StackApp is one node in the stack DAG.
                  properties:
                    build:
                      description: StackAppBuild describes how to compile/containerize
                        an app.
                      properties:
                        buildCommand:
                          type: string
                        javaVersion:
                          type: string
                        nodeVersion:
                          type: string
                        phpVersion:
                          type: string
                        pythonVersion:
                          type: string
                        runtime:
                          type: string
                        tool:
                          type: string
                      type: object
                    config:
                      description: StackInjection is secrets or config envFrom / volumeMounts.
                      properties:
                        envFrom:
                          items:
                            type: string
                          type: array
                        volumeMounts:
                          items:
                            description: StackVolumeMount mounts a Secret or ConfigMap
                              as files.
                            properties:
                              configMap:
                                type: string
                              mountPath:
                                type: string
                              secret:
                                type: string
                            required:
                            - mountPath
                            type: object
                          type: array
                      type: object
                    containerPort:
                      type: string
                    contextDir:
                      type: string
                    dockerfile:
                      type: string
                    downstream:
                      items:
                        type: string
                      type: array
                    name:
                      type: string
                    propagationRole:
                      type: string
                    repo:
                      type: string
                    role:
                      type: string
                    secrets:
                      description: StackInjection is secrets or config envFrom / volumeMounts.
                      properties:
                        envFrom:
                          items:
                            type: string
                          type: array
                        volumeMounts:
                          items:
                            description: StackVolumeMount mounts a Secret or ConfigMap
                              as files.
                            properties:
                              configMap:
                                type: string
                              mountPath:
                                type: string
                              secret:
                                type: string
                            required:
                            - mountPath
                            type: object
                          type: array
                      type: object
                    tests:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - name
                  - repo
                  - role
                  type: object
                minItems: 1
                type: array
              defaults:
                description: Defaults applied to apps when not overridden.
                properties:
                  containerPort:
                    type: string
                  imageRegistry:
                    type: string
                  namespace:
                    type: string
                  servicePort:
                    type: string
                type: object
              description:
                description: Description is optional human-readable text.
                type: string
              gitUrl:
                description: GitURL is the platform / stack definition repository.
                type: string
              name:
                description: Name is the logical stack name (e.g. stack-one).
                minLength: 1
                type: string
              propagation:
                description: Propagation header / baggage settings.
                properties:
                  baggageKey:
                    type: string
                  headerName:
                    type: string
                  strategy:
                    type: string
                type: object
              stackFile:
                description: StackFile is the conventional path used by Tekton params
                  (e.g. stacks/stack-one.yaml).
                type: string
              team:
                description: Team is an optional team label for multi-team installs.
                type: string
            required:
            - apps
            - name
            type: object
          status:
            description: StackStatus defines the observed state of Stack.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              injectionNamespace:
                description: InjectionNamespace is where Secrets/ConfigMaps were checked.
                type: string
              missingConfigMaps:
                description: MissingConfigMaps referenced by apps but absent in the
                  injection namespace.
                items:
                  type: string
                type: array
              missingSecrets:
                description: MissingSecrets referenced by apps but absent in the injection
                  namespace.
                items:
                  type: string
                type: array
              observedGeneration:
                description: ObservedGeneration is the last reconciled metadata.generation.
                format: int64
                type: integer
              topoOrder:
                description: TopoOrder is the resolved app order (entry first).
                items:
                  type: string
                type: array
              valid:
                description: Valid is true when the stack passed structural validation.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
