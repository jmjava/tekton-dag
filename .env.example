# Copy to .env and fill in values. Do not commit .env (it is in .gitignore).
# Used by: configure-github-webhooks.sh, cloudflare-add-tunnel-cname.sh, reporting-gui, create-test-pr.sh, merge-pr.sh

# GitHub (admin:repo_hook or repo scope for webhooks; repo for PR create/merge)
GITHUB_TOKEN=

# EventListener URL for GitHub webhooks (e.g. https://tekton-el.menkelabs.com)
EVENT_LISTENER_URL=

# Cloudflare (for cloudflare-add-tunnel-cname.sh)
CLOUDFLARE_API_TOKEN=
CLOUD_FLARE_ZONE_ID=

# Optional: StorageClass for PipelineRun PVCs. Leave unset or empty for Kind (uses cluster default).
# Set to gp3 (or other) only for AWS. Wrong value (e.g. gp3 on Kind) causes "unbound PersistentVolumeClaims".
# STORAGE_CLASS=
